[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = false
auto_detect_solc = false

# We don't pull forge-std in — the contract is dependency-free.
[fmt]
line_length = 100
tab_width = 4
bracket_spacing = false
